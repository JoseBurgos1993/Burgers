<h1>Welcome to Farmer Joe's Burger Rectangle!</h1>

<h2>Order A New Burger Here</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  <button type="submit">Add</button>
  </div>

</form>

<h2>Burgers that desire consumption!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li>
        {{burger_name}}
        <button class="devour" data-id="{{id}}"> EAT IT!</button>
      </li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been banished to the void!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>
        {{burger_name}}
      </li>
    {{/if}}
  {{/each}}
</ul>


